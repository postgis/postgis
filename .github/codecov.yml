coverage:
    status:
        project:
            default:
                threshold: 0.2%
        patch:
            default:
                threshold: 0.2%
comment: false
ignore:
  - "deps/wagyu/include/**/*" # Ignore wagyu library files
