sudo: required

language: c

services:
    - docker

script:
    - docker build -t postgis_test .
    - docker run postgis_test

notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#postgis-activity"
    on_success: change
    on_failure: always
    use_notice: false
